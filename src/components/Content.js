import Stories from './Stories';
import Posts from './Posts';

export default function Content() {
    return (
        <section id="content">
            <Stories />
            <Posts />
        </section>
    );
}